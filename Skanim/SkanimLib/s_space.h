#pragma once

#include "s_prerequisites.h"

namespace Skanim
{
    /** A enum class to indicate Global and Local Space.
     */
    enum class ESpace
    {
        eGlobal = 0,
        eLocal = 1
    };
};
